"use client";

import { CalendarCheck2, LucideProps } from "lucide-react";
import React from "react"; 

const BookingIcon: React.FC<LucideProps> = (props) => (
  <CalendarCheck2 {...props} />
);

export default BookingIcon;
