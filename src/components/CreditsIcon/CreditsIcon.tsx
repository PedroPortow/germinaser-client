"use client";

import { Coins, LucideProps } from "lucide-react";
import React from "react"; 

const CreditsIcon: React.FC<LucideProps> = (props) => (
  <Coins {...props} />
);

export default CreditsIcon;
